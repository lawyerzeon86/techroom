import { createHmac,timingSafeEqual } from 'node:crypto';
export function verifyWhatsAppSignature(raw:string,header:string|null){const secret=process.env.WHATSAPP_APP_SECRET;if(!secret||!header?.startsWith('sha256='))return false;const expected=createHmac('sha256',secret).update(raw).digest();const supplied=Buffer.from(header.slice(7),'hex');return supplied.length===expected.length&&timingSafeEqual(supplied,expected)}
export async function sendWhatsAppMessage(to:string,text:string,buttonUrl?:string){const token=process.env.WHATSAPP_ACCESS_TOKEN,id=process.env.WHATSAPP_PHONE_NUMBER_ID,version=process.env.WHATSAPP_GRAPH_VERSION||'v23.0';if(!token||!id)return false;const body=buttonUrl?{messaging_product:'whatsapp',to,type:'interactive',interactive:{type:'cta_url',body:{text},action:{name:'cta_url',parameters:{display_text:'Открыть TechRoom',url:buttonUrl}}}}:{messaging_product:'whatsapp',to,type:'text',text:{body:text,preview_url:false}};const r=await fetch(`https://graph.facebook.com/${version}/${id}/messages`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});return r.ok}
