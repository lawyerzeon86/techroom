import {NextResponse} from 'next/server';import{ensureSchema,getPool,rowToProduct}from'../../../../lib/db';import{rateLimit}from'../../../../lib/security';import{maxUserFromRequest}from'../../../../lib/max';
export const runtime='nodejs';export const dynamic='force-dynamic';export async function GET(r:Request){const u=maxUserFromRequest(r);if(!u)return NextResponse.json({error:'Откройте магазин из MAX.'},{status:401});if(!rateLimit(r,`max-products-${u.id}`,120,60000).ok)return NextResponse.json({error:'Слишком много запросов.'},{status:429});try{await ensureSchema();const x=await getPool().query('SELECT * FROM products WHERE is_active=TRUE ORDER BY sort_order,id LIMIT 5000');return NextResponse.json(x.rows.map(rowToProduct))}catch{return NextResponse.json({error:'Каталог временно недоступен.'},{status:503})}}
